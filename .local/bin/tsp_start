#!/bin/sh

notify-send -i task-due -u low "Task Pooler" "Job started in background.\m To stop the job : tsp -k"
# while getopts jobid:error:outfile:command: option; do
#   case $option in
#     (jobid) echo "jobid=> $OPTARG";;
#     (error) echo "error=> $OPTARG";;
#     (outfile) echo "outfile=> $OPTARG";;
#     (command) echo "command=> $OPTARG";;
#   esac
# done
