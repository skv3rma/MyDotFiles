#!/bin/sh

mktouch() {
    echo "creating file(s)"
    if [ $# -lt 1 ]; then
        echo "Missing argument";
        return 1;
    fi

    for f in "$@"; do
        mkdir -p -- "$(dirname -- "$f")"
        touch -- "$f"
        echo "🗇 $f Created Successfully."
    done
}


